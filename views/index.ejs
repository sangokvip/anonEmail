<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600&display=swap">
  <style>
    .title {
      cursor: pointer;
      user-select: none;
    }
  </style>
</head>
<body>
  <div class="container">
    <header>
      <h1 class="title" ondblclick="goToAdmin()">某人要发匿名邮件</h1>
      <p>向狗鸡巴致敬！！！🫡🫡🫡</p>
    </header>

    <main>
      <form id="emailForm" enctype="multipart/form-data">
        <div class="form-group">
          <label for="from">发件人邮箱 (最多5个)</label>
          <div id="senders-container">
            <div class="sender-item">
              <input type="text" class="sender-prefix" placeholder="前缀" required>
              <span class="at-symbol">@</span>
              <select class="sender-domain" required>
                <% domains.forEach(domain => { %>
                  <option value="<%= domain %>"><%= domain %></option>
                <% }); %>
              </select>
              <button type="button" class="btn btn-secondary btn-sm random-sender-prefix">随机生成</button>
            </div>
          </div>
          <button type="button" id="addSender" class="btn btn-outline btn-sm" style="margin-top: 0.5rem;">+ 添加发件人</button>
          <input type="hidden" id="sendersData" name="senders" required>
        </div>

        <div class="form-group">
          <label for="to">收件人邮箱 (最多10个，用逗号分隔)</label>
          <textarea id="to" name="to" placeholder="recipient1@example.com, recipient2@example.com" rows="3" required></textarea>
        </div>

        <div class="form-group">
          <label for="subject">邮件主题</label>
          <div class="input-with-button">
            <input type="text" id="subject" name="subject" placeholder="请输入邮件主题" required>
            <button type="button" id="randomSubject" class="btn btn-secondary btn-sm">随机生成</button>
          </div>
        </div>

        <div class="form-group">
          <label for="content">邮件内容</label>
          <div class="textarea-with-button">
            <textarea id="content" name="content" placeholder="请输入邮件内容" rows="6" required></textarea>
            <button type="button" id="randomContent" class="btn btn-secondary btn-sm">随机生成</button>
          </div>
        </div>

        <div class="form-group">
          <label for="attachments">附件 (最多5个，大小限制10MB)</label>
          <div class="file-upload-container">
            <input type="file" id="attachments" name="attachments" multiple>
            <div class="file-upload-btn btn btn-outline">
              <span>选择文件</span>
            </div>
            <div id="fileList" class="file-list"></div>
          </div>
        </div>

        <div class="form-actions">
          <button type="submit" id="sendButton" class="btn btn-primary btn-lg">发送邮件</button>
        </div>
      </form>

      <div id="resultContainer" class="result-container hidden">
        <div id="result" class="result"></div>
        <button id="closeResultBtn" class="btn btn-outline">关闭</button>
      </div>
    </main>

    <footer>
      <p>&copy; <%= new Date().getFullYear() %> AdamWorks</p>
    </footer>
  </div>

  <script src="/js/script.js"></script>
  <script>
    function goToAdmin() {
      window.location.href = '/admin';
    }
  </script>
</body>
</html> 
 